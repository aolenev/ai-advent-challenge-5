{
  "name": "get_completed_fuelings",
  "description": "Information about completed fuelings stat for period",
  "inputSchema": {
    "type": "object",
    "properties": {
      "from": {
        "type": "number",
        "description": "Start of period. Unix epoch time in milliseconds"
      },
      "to": {
        "type": "number",
        "description": "End of period. Unix epoch time in milliseconds"
      }
    },
    "required": ["from"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "count": {
        "type": "number",
        "description": "Count of completed fuelings"
      },
      "volume": {
        "type": "number",
        "description": "Total volume in liters"
      }
    },
    "required": ["count", "volume"]
  }
}