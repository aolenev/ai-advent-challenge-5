{
  "name": "conversation_response",
  "description": "Provide response for multi-message conversation",
  "input_schema": {
    "type": "object",
    "properties": {
      "isFinished": {
        "type": "string",
        "description": "Is agent provided final response"
      },
      "response": {
        "type": "string",
        "description": "Response for user prompt"
      }
    },
    "required": ["response", "isFinished"]
  }
}